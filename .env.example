# Optional: enables the real Claude-powered AI assistant (npm run serve).
# Without it, the assistant uses the built-in local engine.
# Copy this file to ".env" and fill in your key — do NOT commit ".env".
ANTHROPIC_API_KEY=sk-ant-...
